---
name: trackbed
description: Start or resume Trackbed work on a Jira epic or a standalone project. Determines the anchor (epic key vs project slug), then routes to initialization (if no roadmap exists) or orchestration (if one does).
argument-hint: "<jira-epic-key | project-slug>"
---

Use the trackbed skill to start or resume work on: $ARGUMENTS
